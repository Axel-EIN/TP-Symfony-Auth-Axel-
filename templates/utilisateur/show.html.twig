{% extends 'base.html.twig' %}

{% block html_title %}Profil Utilisateur{% endblock %}

{% block titre %}Profil Utilisateur{% endblock %}

{% block body %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ utilisateur.id }}</td>
            </tr>
            <tr>
                <th>Pseudo</th>
                <td>{{ utilisateur.pseudo }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ utilisateur.roles ? utilisateur.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ utilisateur.password }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-warning" href="{{ path('utilisateur_edit', {'id': utilisateur.id}) }}">Modifier</a>
    
    {{ include('utilisateur/_delete_form.html.twig') }}

    <a href="{{ path('liste_utilisateurs') }}">Retourner à la liste des utilisateurs</a>
{% endblock %}
